# ── GPU em produção (opcional) ──────────────────────────────────────────────────
# Todo detector/OCR pede CUDA automaticamente quando disponível (hardware.py) e cai
# para CPU sem erro se não houver — não precisa mudar código, só trocar pacotes no
# servidor de destino (requer driver NVIDIA + CUDA/cuDNN instalados no SO):
#   pip uninstall onnxruntime && pip install onnxruntime-gpu   # detecção (2 estágios)
#   pip install torch --index-url https://download.pytorch.org/whl/cu121  # EasyOCR
#   pip uninstall paddlepaddle && pip install paddlepaddle-gpu  # PaddleOCR (não
#     testado em GPU real neste projeto — validar após instalar)
# Este ambiente de desenvolvimento é CPU-only; os testes de precisão/latência deste
# projeto foram medidos em CPU (i3-8100T) — em produção com GPU, latência tende a cair
# bastante (detecção e OCR são a maior parte do custo por tentativa no loop de leitura).

# ── Dependências principais ───────────────────────────────────────────────────
bcrypt>=4.2
# Detector de placas — YOLOv9-t ONNX, licença MIT (uso comercial OK).
# Backend padrão (detector_backend = open_image_models). Baixa o modelo no 1º uso.
open-image-models>=0.5
fastapi==0.136.1
uvicorn[standard]==0.46.0
jinja2==3.1.6
python-multipart==0.0.27
# opencv-contrib-python (não headless): o PaddleOCR depende dessa variante. Serve o app
# também (mesmo módulo cv2 + contrib). Substitui o opencv-python-headless.
opencv-contrib-python>=4.10
numpy==2.3.5
onnxruntime==1.25.1
# Detector de VEÍCULO (1º estágio da detecção em 2 estágios, veiculo_dois_estagios_*) —
# YOLOX-s ONNX, licença Apache-2.0. Sem dependência nova: reusa onnxruntime + opencv já
# instalados. Baixe com: python scripts/baixar_modelo.py --veiculo
pytesseract==0.3.13
Pillow==12.2.0
requests==2.33.1

# ── Engine OCR ativo ──────────────────────────────────────────────────────────
# Troque pelo engine selecionado em config.txt (ocr_engine = <valor>).
# Instale apenas UM por vez — cada engine é independente dos outros.

# ocr_engine = easyocr  →  pip install easyocr
easyocr==1.7.2

# OCR de placa de CARRO no modo ocr_engine=auto (self._fast do AutoOCR). Precisa vir
# instalado na imagem: sem ele, o app tenta instalar em runtime como usuário sem
# privilégio (vai para ~/.local, fora do PATH/sys.path do processo) e cai para tesseract,
# que é bem pior para placa de carro. Versão fixada na testada em acurácia (dev).
fast-plate-ocr==1.1.0

# PaddleOCR — reforço do OCR na leitura GET (ocr_leitura_paddle=sim), Apache-2.0.
# Melhora placa antiga borrada (UFPR-ALPR: 49%→64%). Compatível com numpy 2.x.
# Precisa de enable_mkldnn=False (bug do oneDNN no executor PIR em CPU) — já tratado no código.
paddlepaddle>=3.3
paddleocr>=3.7

# ocr_engine = doctr  →  pip install "python-doctr[torch]"
# python-doctr[torch]

# ocr_engine = tesseract  →  sem pacote pip adicional
#   Windows: winget install UB-Mannheim.TesseractOCR
#   Linux:   apt install tesseract-ocr

# ── Ferramentas de desenvolvimento ───────────────────────────────────────────
# benchmark_stream.py usa psutil para medir CPU/memória
# psutil

# ── Reload automático (python main.py --reload) ───────────────────────────────
# Necessário para o uvicorn detectar alterações de arquivo no Windows.
# Sem este pacote o --reload não funciona.
watchfiles>=1.0

# ── Tracker (ByteTrack) ───────────────────────────────────────────────────────
# tracker_ativo = sim  →  pip install boxmot
# Reduz chamadas OCR em ~99% rastreando veículos entre frames.
# Fallback automático para modo clássico se não instalado.
# boxmot
