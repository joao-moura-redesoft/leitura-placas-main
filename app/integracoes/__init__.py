"""Integrações com serviços externos de terceiros.

`operacao/` é operação do próprio servidor (supervisor, retenção) e `seguranca/` é
autenticação. Uma chamada a um provedor externo — e, no caso da apiplacas, PAGA por
consulta — não é nenhum dos dois.
"""
